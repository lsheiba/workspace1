replicas: 1
cpu:
  requests: "8000m"
  limits: "100m"
memory:
  requests: "64Mi"
  limits: "16Gi"
serviceType: ClusterIP
image:
  Name: lsheiba/fast-neural-style
  Tag: latest
persistence:
    enabled: true
    accessMode: ReadWriteOnce
    size: 1Gi
